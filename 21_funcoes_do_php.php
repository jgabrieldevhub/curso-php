<?php
    $nome = ' Senac Americana ';
    var_dump($nome);
    echo '<hr>';

    var_dump(ltrim($nome));
    echo '<hr>';

    /* ltrim = 'remove os espaços do lado esquerdo'; */

    var_dump(rtrim($nome));
    echo '<hr>';

    /* rtrim = 'remove os espaços do lado direito'; */

    var_dump(trim($nome));
    echo '<hr>';

    /* trim = 'remove os espaços de ambos os lados'; */
?>